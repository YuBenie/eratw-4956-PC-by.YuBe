@DEBUG_MODE
DRAWLINE
PRINTL 请选择要修改人物，或进入FLAG/自定义修正
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,CHARANUM
	PRINTFORM [{LOCAL,3,RIGHT}] %CALLNAME:LOCAL,12,LEFT%
	LOCAL:1 += 1
	SIF !(LOCAL:1 % 8)
		PRINTL
NEXT
SIF LOCAL:1 % 8 
	PRINTL
DRAWLINE
PRINT [500] - 全局参数　　　　　
PRINT [501] - 道具修改　　　　　
PRINT [600] - 杂项修改　　　　　
PRINT [700] - 全员回复　　　　　
PRINTL [998] - 更改当前人物
;PRINTFORML [803] - 特殊模式：\@ TFLAG:301 > 0 ? [使用中] # [关闭中] \@
DRAWLINE
PRINTFORML [999] - 离开

WHILE 1
INPUT
IF RESULT == 999
	RETURN 
ELSEIF RESULT == 700
	FOR LOCAL,0,CHARANUM
		BASE:LOCAL:0=MAXBASE:LOCAL:0
		BASE:LOCAL:1=MAXBASE:LOCAL:1
		SIF !HAS_PENIS(LOCAL)
			CONTINUE
		BASE:LOCAL:6=MAXBASE:LOCAL:6
	NEXT
	BASE:0:8=MAXBASE:0:8
	PRINTFORML 全员体力、气力、精力已回复
	PRINTFORMW TSP已回复
	CLEARLINE 3
	CONTINUE
ELSEIF RESULT == 600
	CALL SHOW_OTHER_DEBUG
ELSEIF RESULT == 500
	CALL SHOW_FLAG_DEBUG
ELSEIF RESULT == 501
	CALL ITEM_DEBUG
ELSEIF RESULT == 803
	CALL ESTRUS_DEBUG
ELSEIF RESULT == 998
	CALL TARGET_EDIT
ELSEIF !INRANGE(RESULT,0,CHARANUM)|| NAME:RESULT == ""
	CLEARLINE 1
	CONTINUE
;ELSEIF RESULT == 99
;	CALL WA(1)
;	WAIT
;	RESTART
;ELSEIF RESULT < 14
;	CALL SHOW_COUNTRY_DEBUG,RESULT
;	RESTART
ELSE
	CALL SHOW_SLAVE_DEBUG,RESULT
ENDIF
RESTART
WEND
@SHOW_SLAVE_DEBUG, ARG
DRAWLINE
PRINTL 请选择要修改的数据类型
DRAWLINE
PRINT [ 1] - 基本属性　　　　　
PRINT [ 2] - 扩展属性　　　　　
PRINT [ 3] - 调教状态　　　　　
PRINTL [ 4] - 经验修改
PRINT [ 5] - 能力修改　　　　　
PRINT [ 6] - 素质修改　　　　　
PRINT [ 7] - 刻印修改　　　　　
PRINTL [ 8] - TCVAR修改
PRINT [ 9] - 杂项修改　　　　　
PRINTL [10] - 污渍修改
DRAWLINE
PRINTFORML [999] 离开

WHILE 1

INPUT

IF RESULT == 999
	RETURN 0
ELSEIF RESULT == 1
	CALL BASE_DEBUG,ARG
ELSEIF RESULT == 2
	CALL EX_DEBUG,ARG
ELSEIF RESULT == 3
	CALL PALAM_DEBUG,ARG
ELSEIF RESULT == 4
	CALL EXP_DEBUG,ARG
ELSEIF RESULT == 5
	CALL ABL_DEBUG,ARG
ELSEIF RESULT == 6
	CALL TALENT_DEBUG,ARG
ELSEIF RESULT == 7
	CALL MARK_DEBUG,ARG
ELSEIF RESULT == 8
	CALL TCVAR_DEBUG,ARG
ELSEIF RESULT == 9
	CALL CFLAG_DEBUG,ARG
ELSEIF RESULT == 10
	CALL STAIN_DEBUG,ARG
ELSE
	CLEARLINE 1
	CONTINUE
ENDIF
RESTART
WEND
@SHOW_OTHER_DEBUG


DRAWLINE
PRINTL 请选择要修改的数据类型
DRAWLINE

PRINT [ 1 ] - 所持金钱　　　　　
PRINT [ 2 ] - 所持筹码　　　　　
PRINT [ 3 ] - 更改日期　　　　　
PRINTL [ 4 ] - 更改时间　　　　　
PRINT [ 5 ] - 更改TFLAG 　　　　
PRINT [ 6 ] - 更改天气　　　　　
PRINTL [ 7 ] - 弾幕特能　　　　　
;PRINTL [ 8 ] - 一键演奏　　　　　
DRAWLINE
PRINTFORML [999] 离开

WHILE 1

INPUT

IF RESULT == 999
	RETURN 0
ELSEIF RESULT == 1
	CALL DEBUG_CHANGE_MONEY
ELSEIF RESULT == 2
	CALL DEBUG_CHANGE_MONEY2
ELSEIF RESULT == 3
	CALL DEBUG_CHANGE_DAY
ELSEIF RESULT == 4
	CALL DEBUG_CHANGE_TIME
ELSEIF RESULT == 5
	CALL SHOW_TFLAG_DEBUG
ELSEIF RESULT == 6
	CALL CHANGE_WEATHER_DEBUG
ELSEIF RESULT == 7
	CALL DANMAKU_FEATURE
;ELSEIF RESULT == 8
;	CALL ONE_KEY_MUSIC
;	CONTINUE
ELSE
	CLEARLINE 1
	CONTINUE
ENDIF
RESTART
WEND
@ITEM_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,1000
	SIF ITEMNAME:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL,4,RIGHT}] %ITEMNAME:LOCAL%：{ITEM:LOCAL}
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINT [9999] 离开　　　　　
PRINTFORML [9998]检索

WHILE 1

INPUT

IF RESULT == 9999
	RETURN 0
ELSEIF RESULT == 9998
	CALL SEARCHFOR(1,"ITEM")
	CONTINUE
ELSEIF !INRANGE(RESULT,0,1000)
	CLEARLINE 1
	CONTINUE	
ELSEIF ITEMNAME:RESULT == ""
	CLEARLINE 1
	CONTINUE
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %ITEMNAME:(LOCAL:2)%当前数值为：{ITEM:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	ITEM:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%ITEMNAME:(LOCAL:2)%当前数值为：{ITEM:(LOCAL:2)}
	CONTINUE
ENDIF
RESTART
WEND

@BASE_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,30
	SIF BASENAME:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL,4,RIGHT}] %BASENAME:LOCAL%：{BASE:ARG:LOCAL}
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINTL 以下为最大值（MAXBASE）
FOR LOCAL,0,30
	SIF BASENAME:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL+30,4,RIGHT}] %BASENAME:LOCAL%：{MAXBASE:ARG:LOCAL}
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINTL 请选择要修改的数据
DRAWLINE
PRINT [9999] 离开　　　　　
PRINTFORML [9998]检索
WHILE 1
INPUT
IF RESULT == 9999
	RETURN 0
	ELSEIF RESULT == 9998
	CALL SEARCHFOR(1,"BASE")
	CALL SEARCHFOR(1,"MAXBASE")
	CONTINUE
ELSEIF !INRANGE(RESULT,0,60)
	CLEARLINE 1
	CONTINUE	
ELSEIF RESULT <30 && BASENAME:RESULT == "" 
	CLEARLINE 1
	CONTINUE
ELSEIF RESULT >29 && BASENAME:(RESULT-30) == "" 
	CLEARLINE 1
	CONTINUE
ELSEIF RESULT >29
	LOCAL:2 = (RESULT-30)
	PRINTFORML %BASENAME:(LOCAL:2)%当前数值为：{MAXBASE:ARG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	MAXBASE:ARG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%BASENAME:(LOCAL:2)%当前数值为：{MAXBASE:ARG:(LOCAL:2)}
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %BASENAME:(LOCAL:2)%当前数值为：{BASE:ARG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	BASE:ARG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%BASENAME:(LOCAL:2)%当前数值为：{BASE:ARG:(LOCAL:2)}
ENDIF
RESTART
WEND

@EX_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,100
	SIF EXNAME:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL,4,RIGHT}] %EXNAME:LOCAL%：{EX:ARG:LOCAL}
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINT [9999] 离开　　　　　
PRINTFORML [9998]检索
WHILE 1
INPUT
IF RESULT == 9999
	RETURN 0
ELSEIF RESULT == 9998
	CALL SEARCHFOR(1,"EX")
	CONTINUE
ELSEIF !INRANGE(RESULT,0,100)
	CLEARLINE 1
	CONTINUE	
ELSEIF EXNAME:RESULT == ""
	CLEARLINE 1
	CONTINUE
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %EXNAME:(LOCAL:2)%当前数值为：{EX:ARG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	EX:ARG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%EXNAME:(LOCAL:2)%当前数值为：{EX:ARG:(LOCAL:2)}
ENDIF
RESTART
WEND

@PALAM_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,200
	SIF PALAMNAME:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL,4,RIGHT}] %PALAMNAME:LOCAL%：{PALAM:ARG:LOCAL}
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINT [9999] 离开　　　　　
PRINTFORML [9998]检索
WHILE 1
INPUT
IF RESULT == 9999
	RETURN 0
ELSEIF RESULT == 9998
	CALL SEARCHFOR(1,"PALAM")
	CONTINUE
ELSEIF !INRANGE(RESULT,0,200)
	CLEARLINE 1
	CONTINUE	
ELSEIF PALAMNAME:RESULT == ""
	CLEARLINE 1
	CONTINUE
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %PALAMNAME:(LOCAL:2)%当前数值为：{PALAM:ARG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	PALAM:ARG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%PALAMNAME:(LOCAL:2)%当前数值为：{PALAM:ARG:(LOCAL:2)}
ENDIF
RESTART
WEND
@EXP_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,200
	SIF EXPNAME:LOCAL == ""
		CONTINUE
	LOCALS = [{LOCAL,3}] %EXPNAME:LOCAL%
	LOCALS:2 = ：
	LOCALS:1 = %TOSTR(EXP:ARG:LOCAL)%
	LOCALS += LOCALS:2
	LOCALS += LOCALS:1
	PRINTFORM %LOCALS, 30, LEFT%
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINT [9999] 离开　　　　　
PRINTFORML [9998]检索
WHILE 1
INPUT
IF RESULT == 9999
	RETURN 0
ELSEIF RESULT == 9998
	CALL SEARCHFOR(1,"EXP")
	CONTINUE
ELSEIF !INRANGE(RESULT,0,150)
	CLEARLINE 1
	CONTINUE	
ELSEIF EXPNAME:RESULT == ""
	CLEARLINE 1
	CONTINUE
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %EXPNAME:(LOCAL:2)%当前数值为：{EXP:ARG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	EXP:ARG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%EXPNAME:(LOCAL:2)%当前数值为：{EXP:ARG:(LOCAL:2)}
ENDIF
RESTART
WEND

@ABL_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,100
	SIF ABLNAME:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL,2,RIGHT}] %ABLNAME:LOCAL%：{ABL:ARG:LOCAL}
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINT [9999] 离开　　　　　
PRINTFORML [9998]检索
WHILE 1
INPUT
IF RESULT == 9999
	RETURN 0
ELSEIF RESULT == 9998
	CALL SEARCHFOR(1,"ABL")
	CONTINUE
ELSEIF !INRANGE(RESULT,0,100)
	CLEARLINE 1
	CONTINUE	
ELSEIF ABLNAME:RESULT == ""
	CLEARLINE 1
	CONTINUE
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %ABLNAME:(LOCAL:2)%当前数值为：{ABL:ARG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	ABL:ARG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%ABLNAME:(LOCAL:2)%当前数值为：{ABL:ARG:(LOCAL:2)}
ENDIF
RESTART
WEND

@TALENT_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,300
	SIF TALENTNAME:LOCAL == ""
		CONTINUE
	LOCALS = [{LOCAL,3}] %TALENTNAME:LOCAL%
	LOCALS:2 = ：
	
	IF TALENT:ARG:LOCAL == 0
		LOCALS:1 = 【－】
	ELSE
		LOCALS:1 = 【%GET_TALENTNAME(LOCAL, TALENT:ARG:LOCAL)%】
	ENDIF
	LOCALS += LOCALS:2
	LOCALS += LOCALS:1
	PRINTFORM %LOCALS, 40, LEFT%
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINTFORML [9999] 离开
WHILE 1
INPUT
IF RESULT == 9999
	RETURN 0
ELSEIF !INRANGE(RESULT,0,300)
	CLEARLINE 1
	CONTINUE	
ELSEIF TALENTNAME:RESULT == ""
	CLEARLINE 1
	CONTINUE
ELSE
LOCAL:2 = RESULT
PRINTFORML %TALENTNAME:(LOCAL:2)%当前数值为：{TALENT:ARG:(LOCAL:2)}  【 %GET_TALENTNAME(LOCAL:2, TALENT:ARG:(LOCAL:2))% 】
PRINTL 请输入新的数据
INPUT
TALENT:ARG:(LOCAL:2) = RESULT
PRINTFORMW 修改完毕，%TALENTNAME:(LOCAL:2)%当前数值为：{TALENT:ARG:(LOCAL:2)}   【 %GET_TALENTNAME(LOCAL:2, TALENT:ARG:(LOCAL:2))% 】
ENDIF
RESTART
WEND

@MARK_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,10
	SIF MARKNAME:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL,4,RIGHT}] %MARKNAME:LOCAL%：{MARK:ARG:LOCAL}
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINTFORML [9999] 离开
WHILE 1
INPUT
IF RESULT == 9999
	RETURN 0
ELSEIF !INRANGE(RESULT,0,10)
	CLEARLINE 1
	CONTINUE	
ELSEIF MARKNAME:RESULT == ""
	CLEARLINE 1
	CONTINUE
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %MARKNAME:(LOCAL:2)%当前数值为：{MARK:ARG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	MARK:ARG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%MARKNAME:(LOCAL:2)%当前数值为：{MARK:ARG:(LOCAL:2)}
ENDIF
RESTART
WEND

@CFLAG_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,1000
	SIF CFLAGNAME:LOCAL == ""
		CONTINUE
	PRINTFORM [{LOCAL,3,RIGHT}] %@"%CFLAGNAME:LOCAL%：{CFLAG:ARG:LOCAL}",20,LEFT%
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINT [9999] 离开　　　　　
PRINTFORML [9998]检索

WHILE 1

INPUT

IF RESULT == 9999
	RETURN 0
ELSEIF RESULT == 9998
	CALL SEARCHFOR(1,"CFLAG")
	CONTINUE
ELSEIF RESULT < 0; || RESULT > 999
	CLEARLINE 1
	CONTINUE	
;ELSEIF CFLAGNAME:RESULT == ""
;	CLEARLINE 1
;	CONTINUE
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %CFLAGNAME:(LOCAL:2)%当前数值为：{CFLAG:ARG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	CFLAG:ARG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%CFLAGNAME:(LOCAL:2)%当前数值为：{CFLAG:ARG:(LOCAL:2)}
ENDIF
RESTART
WEND

@TCVAR_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,1000
	SIF TCVARNAME:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL,3,RIGHT}] %@"%TCVARNAME:LOCAL%：{TCVAR:ARG:LOCAL}",20,LEFT%
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINT [9999] 离开　　　　　
PRINTFORML [9998]检索

WHILE 1

INPUT

IF RESULT == 9999
	RETURN 0
ELSEIF RESULT == 9998
	CALL SEARCHFOR(1,"TCVAR")
	CONTINUE
ELSEIF RESULT < 0; || RESULT > 999
	CLEARLINE 1
	CONTINUE	
;ELSEIF TCVARNAME:RESULT == ""
;	CLEARLINE 1
;	CONTINUE
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %TCVARNAME:(LOCAL:2)%当前数值为：{TCVAR:ARG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	TCVAR:ARG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%TCVARNAME:(LOCAL:2)%当前数值为：{TCVAR:ARG:(LOCAL:2)}
ENDIF
RESTART
WEND

@STAIN_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,100
	SIF STAINNAME:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL,4,RIGHT}] %STAINNAME:LOCAL%：{STAIN:ARG:LOCAL}
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINT [9999] 离开　　　　　
PRINTFORML [9998]检索

WHILE 1

INPUT

IF RESULT == 9999
	RETURN 0
ELSEIF RESULT == 9998
	CALL SEARCHFOR(1,"STAIN")
	CONTINUE
ELSEIF RESULT < 0; || RESULT > 99
	CLEARLINE 1
	CONTINUE	
;ELSEIF STAINNAME:RESULT == ""
;	CLEARLINE 1
;	CONTINUE
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %STAINNAME:(LOCAL:2)%当前数值为：{STAIN:ARG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	STAIN:ARG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%STAINNAME:(LOCAL:2)%当前数值为：{STAIN:ARG:(LOCAL:2)}
ENDIF
RESTART
WEND

@SHOW_COUNTRY_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,1000
	SIF CFLAGNAME:LOCAL == ""
		CONTINUE
	PRINTFORMLC [{LOCAL,4,RIGHT}] %CFLAGNAME:LOCAL%：{CFLAG:ARG:LOCAL}
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINTFORML [9999] 离开

WHILE 1

INPUT

IF RESULT == 9999
	RETURN 0
ELSEIF !INRANGE(RESULT,0,999)
	CLEARLINE 1
	CONTINUE	
ELSEIF CFLAGNAME:RESULT == ""
	CLEARLINE 1
	CONTINUE
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %CFLAGNAME:(LOCAL:2)%当前数值为：{CFLAG:ARG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	CFLAG:ARG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%CFLAGNAME:(LOCAL:2)%当前数值为：{CFLAG:ARG:(LOCAL:2)}
ENDIF
RESTART
WEND

@SHOW_FLAG_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
LOCAL:1 = 0
FOR LOCAL,0,10000
	SIF FLAGNAME:LOCAL == ""
		CONTINUE
	LOCALS = [{LOCAL,4}] %FLAGNAME:LOCAL%
	LOCALS:2 = ：
	LOCALS:1 = %TOSTR(FLAG:LOCAL)%
	LOCALS += LOCALS:2
	LOCALS += LOCALS:1
	PRINTFORM %LOCALS, 30, LEFT%
	LOCAL:1 += 1
	SIF LOCAL:1 % 4 == 0
		PRINTL
NEXT
SIF LOCAL:1 % 4 != 0
	PRINTL
DRAWLINE
PRINT [9999] 离开　　　　　
PRINTFORML [9998]检索

WHILE 1

INPUT

IF RESULT == 9999
	RETURN 0
ELSEIF RESULT == 9998
	CALL SEARCHFOR(1,"FLAG")
	CONTINUE
ELSEIF !INRANGE(RESULT,0,1999)
	CLEARLINE 1
	CONTINUE	
ELSEIF FLAGNAME:RESULT == ""
	CLEARLINE 1
	CONTINUE
ELSE
	LOCAL:2 = RESULT
	PRINTFORML %FLAGNAME:(LOCAL:2)%当前数值为：{FLAG:(LOCAL:2)}
	PRINTL 请输入新的数据
	INPUT
	FLAG:(LOCAL:2) = RESULT
	PRINTFORMW 修改完毕，%FLAGNAME:(LOCAL:2)%当前数值为：{FLAG:(LOCAL:2)}
	CONTINUE
ENDIF
RESTART
WEND

@DEBUG_CHANGE_MONEY
PRINTFORML 当前所持金：{MONEY}
PRINTL 所持金改为多少？
PRINTFORML 输入负数则不改动
INPUT
IF RESULT >= 0
	MONEY = RESULT
	PRINTFORMW 现在的所持金:\\{MONEY}
ELSE
	PRINTFORM 未作改动
	PRINTL
	WAIT
	RETURN 0
ENDIF
@DEBUG_CHANGE_MONEY2
PRINTFORML 当前所持筹码：{MONEY:2}
PRINTL 所持筹码改为多少？
PRINTFORML 输入负数则不改动
INPUT

IF RESULT >= 0
	MONEY:2 = RESULT
	PRINTFORMW 现在的所持筹码:\\{MONEY:2}
ELSE
	PRINTFORM 未作改动
	PRINTL
	WAIT
	RETURN 0
ENDIF
@DEBUG_CHANGE_DAY
PRINTFORML 请输入天数。今天是：第{DAY}天
PRINTBUTTON @"[更改日期] ",-1
PRINTBUTTON @"[返回菜单] ",-2
WHILE 1
INPUT
IF RESULT==-1
	PRINTFORML 请输入月份(春夏秋冬1~4)
	WHILE 1
		INPUT
		IF !INRANGE(RESULT,1,4)
			CLEARLINE 1
		ELSE
			DAY:2=RESULT
			BREAK
		ENDIF
	WEND
	PRINTFORML 请输入日期(1~31)	
	WHILE 1
		INPUT
		IF !INRANGE(RESULT,1,31)
			CLEARLINE 1
		ELSE
			DAY:3=RESULT
			BREAK
		ENDIF
	WEND
ELSEIF RESULT >= 0
	DAY = RESULT
	PRINTFORMW 今天的天数已改为：第{DAY}天。
ELSE
	PRINTFORMW 未作改动
	RETURN 0
ENDIF
RESTART
WEND
@ESTRUS_DEBUG,ARG
DRAWLINE
PRINTL 选择要修改的数据
DRAWLINE
PRINTL ★★★当日有效　当日有效　当日有效　次日如需使用起床后请重新开启★★★
PRINTL 
PRINTL 开启恶堕模式后，当日任何接近主角的人
PRINTL 情绪欲情潤滑ＭＡＸ，丧失理性（理性＝０）
PRINTL 忘記對你的壞印象(取消憤怒狀態)，无条件进入发情状态
PRINTL 总之就是湿得一塌糊涂咯！
PRINTL 
PRINTL 后宫模式須先開啟惡墮模式
PRINTL 開啟后宫模式後的效果如下
PRINTL 告白数量无限制
PRINTL 無視生理期，無視體質，無視種族，無視宮填充度，無視宮狀態，無視精質量，一發入魂，必定受孕
DRAWLINE
PRINTFORM [1] - 恶堕模式：\@ TFLAG:301 > 0 ? [已经开启] # [尚未开启] \@　
PRINTFORML [2] - 后宫模式：\@ TFLAG:301 == 2 ? [已经开启] # [尚未开启] \@　
DRAWLINE
PRINTFORML [9999] 离开

WHILE 1

INPUT

IF RESULT == 9999
	RETURN 0
ELSEIF RESULT == 1
	IF TFLAG:301 == 0
		TFLAG:301 = 1
		PRINTL 恶堕模式已开启
		wait
	ELSE
		TFLAG:301 = 0
		PRINTL 恶堕模式已關閉
		wait
	ENDIF
	CONTINUE
ELSEIF RESULT == 2
	IF TFLAG:301 == 1
		TFLAG:301 = 2
		PRINTL 受孕模式已开启
		wait
	ELSEIF TFLAG:301 == 2
		TFLAG:301 = 1
		PRINTL 受孕模式已關閉
		wait
	ELSE
		PRINTL 請先開啟恶堕模式
		wait
	ENDIF
	CONTINUE
ENDIF
RESTART
WEND


@SHOW_TFLAG_DEBUG
LOCAL = 0
VARSIZE TFLAG
LOCAL:9 = RESULT

$PRINT_TFLAG_LIST

PRINTFORMLC (MAX = {LOCAL:9-1})
PRINTL
DRAWLINE
PRINTFORML 現在目前的TFLAG列表
PRINTFORML 请选择需要修改的TFLAG
DRAWLINE
REPEAT LOCAL:9
	SIF STRLENS(TFLAGNAME:COUNT) == 0
		CONTINUE
	PRINTFORMLC [{COUNT, 3}] \@ (STRLENS(TFLAGNAME:COUNT) == 0) ? TFLAG:{COUNT, 3} # %TFLAGNAME:COUNT%\@ = {TFLAG:COUNT}
REND
PRINTL 
DRAWLINE
PRINT [9999] 离开　　　　　
PRINTFORML [9998]检索

WHILE 1

INPUT
IF RESULT == 9999
	RETURN 0
ELSEIF RESULT == 9998
	CALL SEARCHFOR(1,"TFLAG")
	GOTO PRINT_TFLAG_LIST
ELSEIF RESULT < 0 || RESULT >= LOCAL:9
	CONTINUE
ENDIF
RESTART
WEND
LOCAL:1 = RESULT
DRAWLINE
PRINTFORML \@ (STRLENS(TFLAGNAME:RESULT) == 0) ? TFLAG:{RESULT} # %TFLAGNAME:RESULT%\@ 修改為(現在{TFLAG:RESULT})
PRINTFORML 請輸入数値
PRINTLC [99999]返回

INPUT

SIF RESULT == 99999
	RESTART
TFLAG:(LOCAL:1) = RESULT
GOTO PRINT_TFLAG_LIST

@CHANGE_WEATHER_DEBUG
WHILE 1
PRINTL 请选择要修改的天气类型
PRINTL
PRINT [ 0 ] - 标准　　　　　
PRINT [ 1 ] - 異常　　　　　
PRINTL [ 2 ] - 特殊　　　　　
PRINTL
PRINTL [ 99 ]返回

INPUT
IF RESULT == 99
	RETURN
ELSEIF RESULT == 0
	CALL CHANGE_WEATHER_5_DEBUG
	CONTINUE
ELSEIF RESULT == 1
	CALL CHANGE_WEATHER_FLAG_89_DEBUG
	CONTINUE
ELSEIF RESULT == 2
	CALL CHANGE_WEATHER_7_DEBUG
	CONTINUE
ELSE
	CONTINUE
ENDIF
RESTART
WEND

@CHANGE_WEATHER_5_DEBUG
WHILE 1
PRINTL 天气改为哪一种？

PRINT [ 0 ] - 晴天　　　　　
PRINT [ 1 ] - 晴朗　　　　　
PRINT [ 2 ] - 多雲　　　　　
PRINTL [ 3 ] - 陰天　　　　　
PRINT [ 4 ] - 雨　　　　　　
PRINT [ 5 ] - 大雨　　　　　
PRINT [ 6 ] - 霧雨　　　　　
PRINTL [ 7 ] - 霧　　　　　　
PRINT [ 8 ] - 雪　　　　　　
PRINT [ 9 ] - 吹雪　　　　　
PRINT [ 10 ] - 細雪　　　　
PRINTL [ 11 ] - 霧雪　　　　
PRINT [ 12 ] - 雨雪交加　　　
PRINTL [ 13 ] - 冰雹　　　　
PRINTL
PRINTL [ 99 ]返回

INPUT
IF RESULT == 99
	RETURN 0
ELSEIF !INRANGE(RESULT,0,13)
	CONTINUE
ELSE
	TIME:5 = RESULT	
ENDIF
RESTART
WEND
@CHANGE_WEATHER_FLAG_89_DEBUG
WHILE 1
PRINTL 異常気象改为哪一种？

PRINT [ 1 ] - Ｃ敏感　　　　
PRINT [ 2 ] - Ｖ敏感　　　　
PRINT [ 3 ] - Ａ敏感　　　　
PRINTL [ 4 ] - Ｂ敏感　　　　
PRINT [ 5 ] - Ｍ敏感　　　　
PRINT [ 6 ] - 裙子　　　　　
PRINT [ 7 ] - 胖次　　　　　
PRINTL [ 8 ] - 桃色の風　　　
PRINT [ 9 ] - 台風　　　　　
PRINTL [ 10 ] - 猛吹雪　　　
PRINTL
PRINTL [ 0 ] - 無
PRINTL
PRINTL [ 99 ]返回

INPUT
IF RESULT == 99
	RETURN 0
ELSEIF !INRANGE(RESULT,0,10)
	CONTINUE
ELSE
	FLAG:89 = RESULT	
ENDIF
RESTART
WEND
@CHANGE_WEATHER_7_DEBUG
WHILE 1
PRINTL 特殊気象改为哪一种？

PRINT [ 1 ] - 彩虹　　　　　
PRINT [ 2 ] - 钻石星辰　　　
PRINT [ 3 ] - 流星　　　　　
PRINTL [ 4 ] - 流星群　　　　
PRINTL
PRINTL [ 0 ] - 無
PRINTL
PRINTL [ 99 ]返回

INPUT
IF RESULT == 99
	RETURN 0
ELSEIF !INRANGE(RESULT,0,4)
	CONTINUE
ELSE
	TIME:7 = RESULT
ENDIF
RESTART
WEND
@TARGET_EDIT
IF TARGET == 0
	PRINTL 当前没有正在对话的人物
	WAIT
	RETURN 0
ELSE
	PRINTFORML  当前人物是%CALLNAME:TARGET%,序号{TARGET}
	CALL SHOW_SLAVE_DEBUG,TARGET
	RETURN 0
ENDIF

;检索函数___by.LJ
@SEARCHFOR(resultFLAG,SEARCHFORname)
#DIM resultFLAG
#DIM SEARCHFORlog
#DIMS SEARCHFORname
#DIMS ESCname,3
ESCname:0 = 返回
ESCname:1 = ESC
ESCname:2 = esc
PRINTL
$LOOP_SEARCH
DRAWLINEFORM =
PRINTFORML 输入需要检索的%SEARCHFORname%的值\@(resultFLAG == 1) ?（輸入-999999則返回） # （離開請輸入返回/ESC）\@
PRINTFORM %" "*10%
IF resultFLAG == 1
    PRINTBUTTON @" [返回] ",-999999
    INPUT
ELSEIF resultFLAG == 2
    PRINTBUTTON @" [返回] ","返回"
    INPUTS
ENDIF
SEARCHFORlog = 0
IF resultFLAG == 1
    IF RESULT == -999999
        PRINTW 返回
        DRAWLINE
        RETURN
    ELSE
        PRINTFORML %SEARCHFORname%的值為【{RESULT}】的搜尋結果：
        SELECTCASE SEARCHFORname
            CASE "FLAG"
                REPEAT VARSIZE("FLAG")
                    IF FLAG:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%FLAGNAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "CFLAG"
                REPEAT VARSIZE("CFLAG")
                    IF CFLAG:COUNT == RESULT
                        PRINTFORMLC [COUNT]:【%CFLAGNAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "TFLAG"
                REPEAT VARSIZE("TFLAG")
                    IF TFLAG:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%TFLAGNAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "BASE"
                REPEAT VARSIZE("BASE")
                    IF BASE:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%BASENAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "BASE","MAXBASE"
                REPEAT VARSIZE("MAXBASE")
                    IF MAXBASE:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%BASENAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "EXP"
                REPEAT VARSIZE("EXP")
                    IF EXP:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%EXPNAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "ABL"
                REPEAT VARSIZE("ABL")
                    IF ABL:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%ABLNAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "TALENT"
                REPEAT VARSIZE("TALENT")
                    IF TALENT:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%TALENTNAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "PALAM"
                REPEAT VARSIZE("PALAM")
                    IF PALAM:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%PALAMNAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "JUEL"
                REPEAT VARSIZE("JUEL")
                    IF JUEL:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%PALAMNAME:COUNT%の珠】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "EX"
                REPEAT VARSIZE("EX")
                    IF EX:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%EXNAME:COUNT%の珠】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "SOURCE"
                REPEAT VARSIZE("SOURCE")
                    IF SOURCE:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%SOURCENAME:COUNT%の珠】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "EQUIP"
                REPEAT VARSIZE("EQUIP")
                    IF EQUIP:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%EQUIPNAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "TEQUIP"
                REPEAT VARSIZE("TEQUIP")
                    IF TEQUIP:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%TEQUIPNAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "ITEM"
                REPEAT VARSIZE("ITEM")
                    IF ITEM:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%ITEMNAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
                CASE "MARK"
                REPEAT VARSIZE("MARK")
                    IF MARK:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【%MARKNAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "RELATION"
                REPEAT VARSIZE("RELATION")
                    IF RELATION:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【{RELATION:COUNT}】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "STAIN"
                REPEAT VARSIZE("STAIN")
                    IF STAIN:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【{STAIN:COUNT}】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "GOTJUEL"
                REPEAT VARSIZE("GOTJUEL")
                    IF GOTJUEL:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【{GOTJUEL:COUNT}】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "NOWEX"
                REPEAT VARSIZE("NOWEX")
                    IF NOWEX:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【{NOWEX:COUNT}】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "TCVAR"
                REPEAT VARSIZE("TCVAR")
                    IF TCVAR:COUNT == RESULT
                        PRINTFORMLC [{COUNT}]:【{TCVAR:COUNT}】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASEELSE
                RETURN 0
        ENDSELECT
        PRINTL
        PRINTFORML 合計【{SEARCHFORlog}】筆
    ENDIF
ELSEIF resultFLAG == 2
    IF RESULTS == ESCname:0 || RESULTS == ESCname:1 || RESULTS == ESCname:2
        PRINTW 返回
        DRAWLINE
        RETURN
    ELSE
        PRINTFORML %SEARCHFORname%的值為【%RESULTS%】的搜尋結果
        SELECTCASE SEARCHFORname
            CASE "STR"
                REPEAT VARSIZE("STR")
                    IF STRFIND(STR:COUNT,RESULTS,-1) != -1
                        PRINTFORMLC [{COUNT}]:【%STR:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "CSTR"
                REPEAT VARSIZE("CSTR")
                    IF STRFIND(CSTR:TARGET:COUNT,RESULTS,-1) != -1
                        PRINTFORMLC [{COUNT}]:【%CSTR:TARGET:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "TSTR"
                REPEAT VARSIZE("TSTR")
                    IF STRFIND(TSTR:COUNT,RESULTS,-1) != -1
                        PRINTFORMLC [{COUNT}]:【%TSTR:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASE "TRAIN"
                REPEAT VARSIZE("TRAINNAME")
                    IF STRFIND(TRAINNAME:COUNT,RESULTS,-1) != -1
                        PRINTFORMLC [{COUNT}]:【%TRAINNAME:COUNT%】
                        SEARCHFORlog ++
                    ENDIF
                REND
            CASEELSE
                RETURN 0
        ENDSELECT
        PRINTL
        PRINTFORML 合計【{SEARCHFORlog}】筆
    ENDIF
ENDIF
PRINTL
GOTO LOOP_SEARCH

@DANMAKU_FEATURE
DRAWLINE
PRINTFORML %CALLNAME:MASTER%的弾幕特能情况如下
CALL DANMAKU_OPPONENT_STATUS(0, 1)
DRAWLINE
PRINTL 请选择需要添加的弾幕特能
DRAWLINE
PRINT [ 0 ] - 言笑自若　　　
PRINT [ 1 ] - 温柔敦厚　　　
PRINT [ 2 ] - 勇気凛々　　　
PRINTL [ 3 ] - 豪放磊落　　　
PRINT [ 4 ] - 怪力乱神　　　
PRINT [ 5 ] - 洽覧深識　　　
PRINT [ 6 ] - 残忍酷薄　　　
PRINTL [ 7 ] - 乾坤一擲　　　
PRINT [ 8 ] - 狷介孤高　　　
PRINTL [ 9 ] - 幽愁暗恨　　　   
DRAWLINE
PRINTL [ 98 ] - 重置技能　　　
PRINT [ 99 ] - 返回上级　　　
WHILE 1
INPUT
IF RESULT >= 0 && RESULT <= 10
	SETBIT CFLAG:0:弾幕特能, RESULT
ELSEIF RESULT == 98
	CFLAG:0:弾幕特能 = 0
ELSEIF RESULT == 99
	RETURN 0
ELSE
	CLEARLINE 1
ENDIF
RESTART
WEND
@DEBUG_CHANGE_TIME
PRINTFORML 现在时间是：{TIME/60}时{TIME%60}分
CALL INFO_CLOCK_IMAGE(TIME)
PRINTL 请选择或输入要改变的时间长度
PRINTL
PRINTL
PRINT [ -60] -60分　
PRINT [ -30] -30分　
PRINT [ -20] -20分　
PRINT [  20] +20分　
PRINT [  30] +30分　
PRINTL [  60] +60分　
PRINTL
PRINT [ -10] -10分　
PRINT [  -5] -5 分　
PRINT [  -1] -1 分　
PRINT [   1] +1 分　
PRINT [   5] +5 分　
PRINTL [  10] +10分　
PRINTL
PRINT [   0] 归零　 
PRINTL [9999] 返回上级
WHILE 1
INPUT

IF RESULT == 9999
	RETURN 0
ELSEIF !RESULT
	TIME=0
ELSEIF TIME + RESULT < 0 || TIME + RESULT > 1439
	PRINTL 数值超出允许范围
	PRINTW
ELSE
	TIME += RESULT
ENDIF
RESTART
WEND
[SKIPSTART]
@ONE_KEY_MUSIC
CFLAG:20:(300) = CFLAG:0:(300)
CFLAG:21:(300) = CFLAG:0:(300)
CFLAG:22:(300) = CFLAG:0:(300)
CFLAG:94:(300) = CFLAG:0:(300)
CFLAG:95:(300) = CFLAG:0:(300)
CFLAG:96:(300) = CFLAG:0:(300)
RETURN 0

@WA(ARG)
#DIM LIST,200
#DIM COUNTER
COUNTER = 0
FOR LOCAL,1,CHARANUM
	SIF !TALENT:LOCAL:恋人
		CONTINUE
	IF TALENT:LOCAL:恋人 && TALENT:LOCAL:恋人 != ARG
		LIST:COUNTER = LOCAL
		COUNTER ++
	ENDIF
NEXT

PRINTFORM %CALLNAME:(LIST:(RAND:COUNTER))%
[SKIPEND]
;更新日期：2025年3月7日

;FileName_DATE_CMN.ERB ----------------------------- Rev1.00
;約會先名取得関数
;CALL		USER
;ARG		VOID
;RETURN		約會先名
;COMMENT	
;-----------------------------------------------------------
;Custom code, add 1 to make it return the original Japanese
@DATENAME_PLACE_ANON(ARG = 0)
#FUNCTIONS
SIF TFLAG:妄想中 == 1
	RETURNF "妄想"
SIF CHK_DATENOW(CFLAG:MASTER:约会中) == 0
	RETURNF ""
SIF ARG
RETURNF STR:(CFLAG:MASTER:约会中 + 5000)
;Custom code
RETURNF STR_TR(CFLAG:MASTER:约会中 + 5000)

;===================================================

@CHECK_LOVER_NUM
#FUNCTION
#DIM _COUNT
LOCAL = 0
FOR _COUNT, 1, CHARANUM
	SIF TALENT:_COUNT:恋人 > 0
		LOCAL += 1
NEXT
RETURNF LOCAL
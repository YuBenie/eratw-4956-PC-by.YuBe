@ADD_CUSTOM_COM4
IF TALENT:恋慕 || TALENT:恋人
	PRINTL 要如何换呢？
	PRINT [0] - 换上睡衣
	PRINT [1] - 保留内衣
	SIF TALENT:恋人
		PRINT [2] - 全部脱掉
	DO
	INPUT
	SELECTCASE RESULT
	CASE 0
		CALL CTRL_CLOTHES_SET(TARGET, "現在衣装の変更_睡衣")
		CFLAG:TARGET:睡衣 = 1
		PRINTFORML %CALLNAME:PLAYER%向%CALLNAME:TARGET%提出在衣服晾干前先换上睡衣的建议
		PRINTFORM %CALLNAME:TARGET%
		IF TALENT:恋慕 
			IF TALENT:幼児／幼児退行
				PRINTL 点了点头，走到角落处开始操作起来…
			ELSE		
				PRINTL 似是有些兴致寡淡地解起了自己的衣服…
			ENDIF
		ELSEIF TALENT:思慕
			IF TALENT:幼児／幼児退行
				PRINTL 点了点头，走到角落处开始操作起来…
			ELSEIF TALENT:動物耳
				PRINTL 扭捏着身子，最后还是把身子背了过去…
			ELSE
				IF TARGET == 57 && !TALENT:恋慕
					PRINTFORM 切出猿猴面具
				ELSE
					PRINTFORM 脸上泛红
				ENDIF
				PRINTL 的同时，对建议本身似乎也不是很不满的样子…
			ENDIF
		ELSE
			IF TALENT:幼児／幼児退行
				PRINTL 点了点头，走到角落处开始操作起来…
			ELSEIF TALENT:動物耳
				PRINTL 确定安全后才将身子转到一边…
			ELSE
				IF TARGET == 57
					PRINTFORM 切出狮子面具，
				ELSE
					PRINTFORM 带着警惕的神情，
				ENDIF
				PRINTL 看向自己湿漉漉的衣服…
			ENDIF
		ENDIF
	CASE 1
		CALL CTRL_CLOTHES_SET(TARGET, "現在衣装の変更_下着姿")
		CALL CLOTHES_SETTING_TRAIN(TARGET)
		PRINTFORML %CALLNAME:PLAYER%向%CALLNAME:TARGET%建议先把外衣脱下放一边免得着凉
		PRINTFORM %CALLNAME:TARGET%
		IF TALENT:恋人
			PRINTL 呼了口气，然后按着需求开始了宽衣解带…
		ELSE
			IF TALENT:幼児／幼児退行
				PRINTL 点了点头，走到角落处开始操作起来…
			ELSEIF TALENT:動物耳
				PRINTL 身体一惊，但还是满脸通红地宽衣解带起来…
			ELSE
				IF TARGET == 57
					PRINTFORM 切出猿猴面具，
				ELSE
					PRINTFORM 神情满是羞臊，但还是
				ENDIF
				PRINTL 开始剥下身上湿漉漉的布料…
			ENDIF
		ENDIF
	CASE 2
		CALL CTRL_CLOTHES_SET(TARGET, "現在衣装の変更_全裸")
		CALL CLOTHES_SETTING_TRAIN(TARGET)
		PRINTFORML %CALLNAME:PLAYER%向%CALLNAME:TARGET%提议把身上的所有衣服都好好晾晾
		PRINTFORM 知道%CALLNAME:PLAYER%意图的%CALLNAME:TARGET%
		IF TARGET == 57
			PRINTFORM 切出猿猴面具，
		ELSE
			PRINTFORM 一边羞涩地微笑着着向%CALLNAME:PLAYER%，一边
		ENDIF
		PRINTL 将自己的手放在了衣服扣子上…
	CASEELSE
		CONTINUE
	ENDSELECT
	BREAK
	LOOP 1
ELSE
	CALL CTRL_CLOTHES_SET(TARGET, "現在衣装の変更_睡衣")
	CFLAG:TARGET:睡衣 = 1
	PRINTFORML %CALLNAME:PLAYER%向%CALLNAME:TARGET%提出在衣服晾干前先换上睡衣的建议
	PRINTFORM %CALLNAME:TARGET%
	IF TALENT:恋慕 
		IF TALENT:幼児／幼児退行
			PRINTL 点了点头，走到角落处开始操作起来…
		ELSE		
			PRINTL 似是有些兴致寡淡地解起了自己的衣服…
		ENDIF
	ELSEIF TALENT:思慕
		IF TALENT:幼児／幼児退行
			PRINTL 点了点头，走到角落处开始操作起来…
		ELSEIF TALENT:動物耳
			PRINTL 扭捏着身子，最后还是把身子背了过去…
		ELSE
			IF TARGET == 57 && !TALENT:恋慕
				PRINTFORM 切出猿猴面具
			ELSE
				PRINTFORM 脸上泛红
			ENDIF
			PRINTL 的同时，对建议本身似乎也不是很不满的样子…
		ENDIF
	ELSE
		IF TALENT:幼児／幼児退行
			PRINTL 点了点头，走到角落处开始操作起来…
		ELSEIF TALENT:動物耳
			PRINTL 确定安全后才将身子转到一边…
		ELSE
			IF TARGET == 57
				PRINTFORM 切出狮子面具，
			ELSE
				PRINTFORM 带着警惕的神情，
			ENDIF
			PRINTL 看向自己湿漉漉的衣服…
		ENDIF
	ENDIF
ENDIF
TCVAR:TARGET:EVENT_CLOTHING_SET = 1
TIME += 3
湿身度:TARGET = 0 

@ADD_CUSTOM_COM_ABLE4
SIF !TFLAG:100  ;会话实行判定
	RETURN 0
SIF FLAG:70 ;时停状态还要拿熨衣服当扒衣服理由？回答我！Look in my eyes!
    RETURN 0
SIF CFLAG:うふふ	;做爱的时候有另外的脱衣逻辑
	RETURN 0
SIF !湿身度:TARGET   ;没湿身脱啥
    RETURN 0
SIF !Tequip:0 && !Tequip:1   ;已经全裸了就用不着了
    RETURN 0
SIF !(IN_HOME(CFLAG:TARGET:300) || IN_HOME(CFLAG:MASTER:300)) || OUTROOF(CFLAG:MASTER:300)   ;在别人家里或在店里换衣服吗 那可太酷了
    RETURN 0
RETURN 1

@ADD_CUSTOM_MESSAGE_COM4
@QUICKFILTER_CUSTOM(ID_COM)
#FUNCTION
#DIM ID_COM
#DIM CONST BIT_Ｖ系       = 1
#DIM CONST BIT_Ａ系       = 2
#DIM CONST BIT_奉仕系     = 5
#DIM CONST BIT_SM系       = 6
#DIM CONST BIT_セクハラ系 = 7
#DIM CONST BIT_家事系     = 9
#DIM CONST BIT_自慰系     = 13  ;たぶんつかってない
;SIF !QUICKFILTER(ID_COM)
;	RETURNF 0
SIF IS_CUSTOM_COM_VAGINA(ID_COM)   && !CFLAG:TARGET:Ｖ過濾器
	RETURNF 0
SIF IS_CUSTOM_COM_ANAL(ID_COM)     && !CFLAG:TARGET:Ａ過濾器
	RETURNF 0
SIF IS_CUSTOM_COM_HOUSI(ID_COM)    && !CFLAG:TARGET:奉仕過濾器
	RETURNF 0
SIF IS_CUSTOM_COM_SM(ID_COM)       && !CFLAG:TARGET:ＳＭ過濾器
	RETURNF 0
SIF IS_CUSTOM_COM_KAJI(ID_COM)     && !FLAG:家事過濾器
	RETURNF 0
SIF IS_CUSTOM_COM_SEKUHARA(ID_COM) && !CFLAG:TARGET:性騷擾過濾器 && (!FLAG:70 || CFLAG:TARGET:344 == 1)
	RETURNF 0
;===========================
RETURNF 1

@IS_CUSTOM_COM_VAGINA(ID_COM)
#FUNCTION
#DIM ID_COM
;Custom commands involving vaginal sex go here
; SIF GROUPMATCH(ID_COM)
; 	RETURNF 1
RETURNF 0

@IS_CUSTOM_COM_ANAL(ID_COM)
#FUNCTION
#DIM ID_COM
;Custom commands involving anal sex go here
; SIF GROUPMATCH(ID_COM)
; 	RETURNF 1
RETURNF 0

@IS_CUSTOM_COM_HOUSI(ID_COM)
#FUNCTION
#DIM ID_COM
;Custom commands involving (You) servicing a character go here
SIF GROUPMATCH(ID_COM, 9228)
	RETURNF 1
RETURNF 0

@IS_CUSTOM_COM_SM(ID_COM)
#FUNCTION
#DIM ID_COM
;Custom commands involving S&M go here
;SIF GROUPMATCH(ID_COM)
;	RETURNF 1
RETURNF 0

@IS_CUSTOM_COM_SEKUHARA(ID_COM)
#FUNCTION
#DIM ID_COM
;Custom commands involving petting go here
SIF GROUPMATCH(ID_COM,9004)
	RETURNF 1
RETURNF 0

@IS_CUSTOM_COM_KAJI(ID_COM)
#FUNCTION
#DIM ID_COM
;Custom commands involving housekeeping(cleaning, fetching water, resting, etc.) go here
;SIF GROUPMATCH(ID_COM)
;	RETURNF 1
RETURNF 0


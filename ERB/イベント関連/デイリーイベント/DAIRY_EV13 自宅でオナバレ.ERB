@DAILY_CONDITION_EV13
#DIM 対象

SIF RAND:(10 - TALENT:MASTER:劣情察知)> 0
	RETURN -1
対象 = 0
CALL FISHER_YATES_SHAFFLE(CHARANUM - 1)
FOR LOCAL:10, 0, CHARANUM - 1
	LOCAL = SHAFFLE_ARRAY:(LOCAL:10) + 1
	SIF !CFLAG:LOCAL:神社在住
		CONTINUE
	IF CFLAG:LOCAL:积攒度 >  700
		対象 = LOCAL
		BREAK
	ENDIF
NEXT
SIF !対象
	RETURN -1

CALL DAILY_EV13(対象)
RETURN 1


@DAILY_EV13(ARG)
PRINTFORMW 在半夜醒了過來
PRINTFORMW 想繼續睡覺但是怎麼也睡不著了……
CALL ASK_YN("散步","待在房間")
IF RESULT
	PRINTFORM %CALLNAME:MASTER%直到重新発困為止
	PRINTDATA
		DATAFORM 整理・鑑賞著自己的收藏
		DATAFORM 看著美丽的胖次陷入了沉思
		DATAFORM 看着墙上的斑点发愣
	ENDDATA
	PRINTFORML ……
	CALL COLORMESSAGE("心灵受到了洗礼、气力和TSP暂时的增加了",C_YELLOW,2,1)
	CALL BUFF_BASE(MASTER,BASE_気力,100)
	CALL BUFF_BASE(MASTER,BASE_TSP,100)
ELSEIF !RAND:4 || (!RAND:4 && ABL:ARG:親密 >= 7)
	PRINTFORMW 隨意散步了一會、\@ CFLAG:MASTER:311 == CFLAG:ARG:311 ? 在打算回自己房間的時候、聽到了什麼聲音# 想著差不多該回去的時候、聽到從%CALLNAME:ARG%的房間傳來了什麼聲音於是打算去看看\@
	CALL ONABARE(ARG)
	CFLAG:MASTER:徹夜 = 1
ELSEIF ITEM:兎符「開運大紋」
		PRINTL 咦、開运大纹的様子…？
		CALL ASK_YN("取出","置之不理")
		IF !RESULT
			ITEM:兎符「開運大紋」 --
			SETCOLOR COLOR ("YELLOW")
			PRINTFORMW 兎符「開運大紋」発動！
			RESETCOLOR
			PRINTFORMW 隨意散步了一會、\@ CFLAG:MASTER:311 == CFLAG:ARG:311 ? 在打算回自己房間的時候、聽到了什麼聲音# 想著差不多該回去的時候、聽到從%CALLNAME:ARG%的房間傳來了什麼聲音於是打算去看看\@
			CALL ONABARE(ARG)
			CFLAG:MASTER:徹夜 = 1
		ELSEIF !RAND:2
			GOTO OYATU
		ELSE
			GOTO NANIMONAI
		ENDIF
ELSEIF !RAND:2
	$OYATU
	PRINTFORML 肚子有点饿了。而且，吃点什么或许会助眠。
	PRINTFORMW %CALLNAME:MASTER%去了厨房，翻找橱柜，发现了一份看起来很美味的点心。
	PRINTFORMW 这不是自己买的。然而，也不能排除是某人特意为自己准备的可能性。
	PRINTFORML 
	CALL COLORMESSAGE("怎么办，保质期马上就要到了！有没有人快点来吃掉我啊！",C_YELLOW,2,2)
	PRINTFORML 
	PRINTFORML 点心也这么说了。那就恭敬不如从命地享用吧。
	CALL BUFF_BASE(MASTER,BASE_気力,300)
	CALL COLORMESSAGE("吃了好吃的点心、一段时间内气力增加",C_YELLOW,2,1)
	PRINTFORMW %CALLNAME:MASTER%重新刷牙、高兴地睡着了。
	CALL COLORMESSAGE(@"不知道为什么、%CALLNAME:ARG%的心情变差了……",C_RED,2,1)
	TALENT:ARG:心情 = -1
ELSE
	$NANIMONAI
	PRINTFORMW 出去闲逛后发现好像没有什么有趣的事、于是决定老老实实的回去睡觉了
	CFLAG:MASTER:就寝時間 += 60
	CALL COLORMESSAGE("睡眠時間減少了1小時",C_YELLOW,2,1)
ENDIF
